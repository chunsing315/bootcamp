# bootcamp

## Java Project
- 1. Inject Java Resource/Class into the project
    e.g. import (it is per class)
    e.g. BigDecimal.calss String.class StringBuilder.class already injected into all Java Projects
    e.g. Java 8 + : you have Stream.class.  (if you are at Java 5, you can not import Java 5)
- 2. Import the class into your class (Manual import - BigDecimal)
- what is not in jdk? your custom class
- 3. Maven can help with non java-lang Classes