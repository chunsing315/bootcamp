package com.bootcamp.tdd;

import org.junit.Test;

public class CalculatorTest {
    
    // requirement change: if number of integer > 2, return the first element
    @Test
    void testSum(){
        assertEquals(15,new Calculator(Arrays.asList(3,10,3).sum);
        assertEquals(15,new Calculator(Arrays.asList(3,10,3).sum);
        assertEquals(15,new Calculator(Arrays.asList(3,10,3).sum);
    }
}
